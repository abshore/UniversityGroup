{#<table border="1">
    <tr>
        <td>Jours/Seances</td>
    </tr>
        {% for item in jours %}
    <tr><td>{{item}}</td>
        {% for a in act %}
            {% if (a==item) %}
            <td>bien</td>
            {%endif%}
           {% endfor %} 
    </tr>
            {% endfor %}


</table>
#}
<form id="form" method="POST" action="{{ path('emploi_emploiClasse') }}">
<select id="classes" name="classe" url="{{ path('emploi_emploiClasse') }}">
    <option id=""></option>
    {% for classe in classes %}
    <option value="{{classe.id}}">{{classe.code}}</option>
    {% endfor %}
</select>
        <button type="submit">envoyer</button>
</form>

<iframe id="iframe" onload="emploi();" style="width: 100%; height: 500px;" frameborder="no"></iframe>
<div id="emploi" style="display: none"></div>
<script src="{{ asset('js/jquery-1.9.1.js')}}"></script>
    <script src="{{ asset('js/jquery-ui.js') }}"></script>
 <script type="text/javascript">
     function emploi(){
         $('#classes').change(function() {
             var classe=$(this).val();
             $.post("{{ path('emploi_emploiClasse') }}",
                       { classe: classe
                       },function(){
                 
             });
                //$('#iframe').attr('src', $(this).attr('url'));
        });
     }
        

        
        </script>